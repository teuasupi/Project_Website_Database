export const API_ENDPOINTS = {
  USERS: '/api/users',
  AUTH: '/api/auth',
  POSTS: '/api/posts',
} as const;

export const USER_ROLES = {
  ADMIN: 'admin',
  ALUMNI: 'alumni',
  STUDENT: 'student',
} as const;
